# interpreter
Build a simple interpreter step by step.

## Usage

```sh
go build -o interpreter .
./interpreter -h
```
> Or you can use `make` to run Makefile in this repo.

Get more details in partX/readme.md
